version: "3.8"

networks:
  appnet:
    external: true

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_BASE_URL: /api
    container_name: bbp-web
    restart: unless-stopped
    environment:
      TZ: Asia/Bangkok
    ports:
      - "18000:80"
    networks: [appnet]
